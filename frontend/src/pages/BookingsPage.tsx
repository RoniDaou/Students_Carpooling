import {useEffect,useState} from "react";import Layout from "@/components/layout/Layout";import {useRides} from "@/contexts/RideContext";import {RideRequest} from "@/types";import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";
export default function BookingsPage(){const{myRequests}=useRides();const[items,setItems]=useState<RideRequest[]>([]);useEffect(()=>{myRequests().then(setItems)},[]);return <Layout><div className="container mx-auto p-6"><h1 className="text-3xl font-bold mb-6">My ride requests</h1><div className="grid md:grid-cols-2 gap-4">{items.map(x=>{const r=x.rideId;return <Card key={x._id}><CardHeader><CardTitle>{r.pickupLocation} → {r.destination}</CardTitle></CardHeader><CardContent><p>{new Date(r.date).toLocaleString()}</p><p className="capitalize font-semibold">Status: {x.status}</p></CardContent></Card>})}{!items.length&&<p>No requests yet.</p>}</div></div></Layout>}
