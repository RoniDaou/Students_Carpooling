import {useState} from "react";import Layout from "@/components/layout/Layout";import {useAuth} from "@/contexts/AuthContext";import {Input} from "@/components/ui/input";import {Button} from "@/components/ui/button";import {Card,CardContent,CardHeader,CardTitle} from "@/components/ui/card";import {useToast} from "@/hooks/use-toast";
export default function ProfilePage(){const{user,updateProfile}=useAuth();const{toast}=useToast();const[busy,setBusy]=useState(false);if(!user)return null;const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();const f=new FormData(e.currentTarget);setBusy(true);try{await updateProfile({first_name:String(f.get("first_name")),last_name:String(f.get("last_name")),phoneNumber:String(f.get("phoneNumber")),campusLocation:String(f.get("campusLocation")),location:String(f.get("location")),vehicleNumber:String(f.get("vehicleNumber")||"")});toast({title:"Profile updated"})}catch(e){toast({title:"Update failed",description:(e as Error).message,variant:"destructive"})}finally{setBusy(false)}};return <Layout><div className="container max-w-xl mx-auto p-6"><Card><CardHeader><CardTitle>My profile</CardTitle></CardHeader><CardContent><form onSubmit={submit} className="space-y-4"><Input name="first_name" defaultValue={user.first_name}/><Input name="last_name" defaultValue={user.last_name}/><Input disabled value={user.universityEmail}/><Input name="phoneNumber" defaultValue={user.phoneNumber}/><Input name="campusLocation" defaultValue={user.campusLocation}/><Input name="location" defaultValue={user.location}/>{user.role==="driver"&&<Input name="vehicleNumber" defaultValue={user.vehicleNumber}/>}<Button disabled={busy} className="w-full bg-lau-green">Save changes</Button></form></CardContent></Card></div></Layout>}
